# Playtime Pact 0.61.0-rc.9

2026-09-15 · Windows x64 · 미서명 테스트 릴리스

## 변경 사항

- Windows 네이티브 API로 `java.exe`/`javaw.exe`의 실제 실행 명령을 읽어, 공용 Java 경로를 사용하는 Minecraft도 게임 인자에서 식별합니다.
- 실행 명령을 읽을 수 없을 때는 Minecraft/Lunar 런처의 자식 Java 프로세스를 추적하는 보조 감지를 사용합니다.
- Minecraft Launcher, Lunar, Badlion, Feather, Prism, MultiMC, PolyMC, HMCL, PCL2 런처 창만 열린 상태는 게임 시간으로 계산하지 않습니다.
- 표준 계정의 AppLocker 설치 제한을 유지하면서 현재 설치된 Lunar/Minecraft Java 런타임만 허용하는 `scripts/allow-existing-game-runtimes.ps1` 관리자 도구를 추가했습니다.
- 이 PC의 `joonw` 계정에서 Lunar Java 17·21·25 런타임 6개를 게시자·제품·파일명·주 버전으로 허용했습니다. Store·App Installer·MSI 제한은 유지됩니다.

- 창 제목 영역을 드래그해 창을 이동할 수 있도록, 상위 컨테이너가 드래그 영역을 막던 스타일을 수정했습니다.
- 기본 규칙 저장 시 보호 서비스의 저장 결과를 모든 창에 전달하고, 메인으로 돌아올 때 시간과 횟수를 새로 불러옵니다.
- 평일·주말 하루 횟수에 0회를 허용합니다. 보호 서비스와 설정 파일도 0회를 유지합니다. 기존 사용량을 지우지 않고, 허용량을 줄인 날에도 사용량을 읽을 수 있습니다.
- 시작 승인 옵션을 켰을 때 온라인/오프라인 모두 PC 부모 PIN으로 다음 게임 실행을 승인할 수 있습니다. 승인은 5분 이내 새 실행 한 번에만 적용되며, 허용 시간대·잔여 시간·횟수 검사는 그대로 유지됩니다. 연결된 모바일 승인도 유지됩니다.
- 메인에 '오늘 시간 추가'와 '부모님 관리 · 기본 설정'을 배치했습니다.
- 관리 화면을 '기본 규칙 / 오늘 시간 / PIN·기타'로 통합했습니다. 같은 창의 부모 인증을 5분간 재사용하고, '잠금'으로 즉시 잠글 수 있습니다. 시간 추가 화면의 큰 장식과 중복 제목을 줄였습니다.
- 기본 시간을 다 쓰기 전에도 부모가 오늘의 시간을 추가할 수 있습니다. 시간 추가는 보호된 부모 인증과 사용량 저장을 거칩니다.
- 설치 프로그램에서 보호 서비스 준비 확인을 최대 5회 재시도합니다. 실패하면 완료로 표시하지 않고 진단 로그를 남깁니다. 이전 설치 실패로 서비스 등록이 사라진 경우의 제거·복구 처리도 보완했습니다.
- 제거 PIN 검사를 PowerShell 대신 네이티브 도구로 처리합니다. SYSTEM 전용 PIN 파일은 임시 LocalSystem 서비스와 보호된 named pipe를 통해 일치 여부만 확인합니다.
- 0.60.8과 rc.1~rc.5 업그레이드에서는 구버전 제거기를 승인된 in-place 정리 도구로 교체해 감시 프로그램의 앱 재실행과 파일 잠금 오류를 피합니다.
- 이미 멈췄거나 없는 서비스도 정상적인 제거 조건으로 처리하고, 보호 사용량 파일의 잘못된 소유권·ACL을 내용 삭제 없이 자동 복구합니다.
- Windows에서 CLI 스크립트의 첫 줄 및 줄바꿈 차이 때문에 실패하던 테스트 실행·비교를 보완했습니다.
- Roblox/Minecraft 감지를 PowerShell/CIM에서 Windows 네이티브 API로 교체해 제한 언어 정책에서도 동작하도록 했습니다.
- 게임 종료도 PID·실행 파일·시작시각을 네이티브로 재검증한 뒤 일반 종료와 강제 종료 순서로 처리합니다.
- 현재 로그인 세션의 게임만 감지하고, 다른 Windows 계정에서 실행 중인 게임은 계산하거나 차단하지 않습니다.
- 수동 실행은 메인 화면을 표시하고 모든 사용자용 바탕 화면 바로가기를 설치합니다. HKLM 자동 실행은 WSH/VBS를 경유하지 않고 앱을 직접 실행해 표준 계정을 포함한 각 로그인 계정에서 백그라운드 타이머를 시작합니다.

## 설치 오류와 서명은 별개입니다

'The protected Playtime service failed its IPC health check'는 앱 보호 서비스 응답 확인 실패입니다. 이번 PC에서는 보호 사용량 파일의 접근 권한 오류를 원인으로 확인했고, LocalSystem ACL 복구 후 서비스와 IPC readiness가 정상화됐습니다.

이 파일은 **Authenticode 미서명(NotSigned)** 상태입니다. 자체 서명을 붙여도 다른 PC의 Windows 신뢰 경고가 사라지는 것은 아닙니다. SmartScreen, Smart App Control 또는 조직 정책이 차단할 수 있으며, 보안 기능 해제나 신뢰 인증서 강제 설치를 하지 않습니다.

Microsoft 안내: https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation

## 검증 결과와 남은 검증

- TypeScript 검사, 앱 빌드, Windows NSIS 설치 파일 생성: 통과.
- 전체 자동 검사: 데스크톱/Vitest 319개 통과·5개 건너뜀, Node 통합 127개 통과·2개 건너뜀, 실패 0개.
- Minecraft/Lunar 감지, 설치 복구 계약과 표준 계정 정책 도구 핵심 검사: 45개 통과.
- 설치 안전성·패키지 계약 핵심 테스트: 37개 통과.
- 이 PC에서 rc.7 → rc.8 및 rc.8 동일 버전 교체 설치: 통과. 설치 종료 코드 0, 설치 버전 0.61.0-rc.8, 보호 서비스 RUNNING, IPC readiness 종료 코드 0을 확인했습니다.
- rc.9 전체 자동 검사를 Windows 정책 아래에서 끝까지 실행했습니다.
- 네이티브 프로세스 감지·동일성 검증 종료, 시작 표시, 패키지·부모 인증 IPC 핵심 재검사: 7개 파일 58개 통과.
- 설정 변경 반영, 0회 저장, 온라인 PC PIN 승인, 인증 재사용, 빠른 시간 추가의 화면 상호작용 테스트를 포함합니다.
- Electron 모의 데이터 화면 9종 생성·점검. 메인과 관리 화면의 드래그 영역 CSS를 확인했습니다. 이 PC에서 실제 Roblox 감지, 승인 전 종료, 부모 PIN 승인 후 재실행, 타이머 상태 생성, 남은 시간 만료 후 자동 종료까지 확인했습니다.
- 원격 백엔드의 실제 로컬 Worker/D1 종단 검사까지 포함한 Node 통합 테스트를 끝까지 완료했습니다.
- 배포용 실행 의존성 검사: npm audit --omit=dev 기준 알려진 취약점 0개. 개발·빌드 도구 전체의 취약점 0개를 의미하지 않습니다.
- 공용 바탕 화면·시작 메뉴 바로가기와 모든 사용자 HKLM 자동 실행 등록은 이 PC에서 확인했습니다. `joonw` 계정의 Lunar Java 런타임 6개는 정책상 `Allowed`로 확인했지만, 표준 계정 로그인 후 rc.9로 Lunar/Minecraft를 실제 실행하는 전체 흐름과 다른 PC 신규 설치는 아직 최종 확인이 필요합니다. 이 때문에 정식 최신 릴리스로 승격하지 않습니다.
- 부모 PIN·기본 규칙·사용량은 rc.8 교체 설치 중 초기화하지 않았습니다.

## 부모님이 확인할 순서

1. 게임을 모두 닫고 설치 파일을 실행합니다. Windows 관리자 승인과 기존 부모 PIN 입력은 부모가 직접 진행합니다.
2. '부모님 관리'에서 평일·주말 시간/횟수를 저장하고 메인 표시가 바뀌는지 확인합니다.
3. 현재 요일의 횟수를 0회로 설정해 기본 게임이 시작되지 않는지 확인합니다. 이미 승인된 진행/일시정지 시간과 부모가 별도로 추가한 시간은 삭제하지 않습니다.
4. 메인의 '오늘 시간 추가'에서 5분을 추가합니다.
5. 시작 승인 옵션을 켜고 PC PIN으로 승인한 뒤 5분 안에 새 게임을 실행합니다.
6. 게임 감지, 경고, 시간 만료 종료를 부모 입회하에 확인합니다. 문제가 생기면 관리자 화면에서 게임을 중지합니다.

설치 실패 시 진단 경로: C:\ProgramData\PlaytimePact\install-health.log
부모 PIN 원문, admin-secret.json, 비밀 키 파일은 공유하지 마세요.

## 다운로드 무결성

Playtime Pact Setup 0.61.0-rc.9.exe
SHA256: 2116DF99C047BAB9684B28AE9051E9F8A93128BE4C1FDEC7B3AFEC64D202193F

파일 크기: 102,474,688 bytes.
